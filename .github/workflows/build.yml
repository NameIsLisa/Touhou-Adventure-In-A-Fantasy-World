name: Debug RenPy Path

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Show workspace structure
        run: |
          echo "=== Root directory ==="
          dir $env:GITHUB_WORKSPACE
          echo "=== game directory ==="
          dir $env:GITHUB_WORKSPACE\game
